# skyboard
A real-time flight tracking tool built with Python. Uses ADSB data to detect the closest commercial aircraft overhead, enriches it with static airline/airport metadata, and outputs the final route, aircraft information, and speed to both the CLI and a connected LED signboard.
